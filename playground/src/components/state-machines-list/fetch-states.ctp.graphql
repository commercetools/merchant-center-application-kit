query FetchStates(
  $locale: Locale!
  $limit: Int!
  $offset: Int!
  $sort: [String!]
) {
  states(limit: $limit, offset: $offset, sort: $sort) {
    total
    count
    offset
    results {
      id
      name(locale: $locale)
      key
    }
  }
}
