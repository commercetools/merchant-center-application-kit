{
  "applicationName": "merchant-center-application-template-starter",
  "frontendHost": "${env:APP_HOSTNAME}",
  "mcApiUrl": "https://${env:MC_API_HOSTNAME}",
  "location": "${env:MC_LOCATION}",
  "env": "development",
  "cdnUrl": "http://${env:APP_HOSTNAME}",
  "servedByProxy": false
}
