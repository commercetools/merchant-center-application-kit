import { defineMessages } from 'react-intl';

export default defineMessages({
  readOnly: {
    id: 'Messages.readOnly',
    description: 'The title for read only access',
    defaultMessage: 'View access only',
  },
});
