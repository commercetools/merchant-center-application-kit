# url-utils

See root `AGENTS.md` for monorepo-wide context.

## Purpose

Provides lightweight URL utility functions (`trimLeadingAndTrailingSlashes`, `joinPaths`) used by other application-kit packages for constructing route paths.

## How To Work Here

Follow root conventions. Tests: `pnpm --filter @commercetools-frontend/url-utils run test`
