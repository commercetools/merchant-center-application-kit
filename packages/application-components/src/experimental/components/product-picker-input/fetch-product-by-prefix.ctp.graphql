query FetchProductByPrefix($where: String!) {
  products(where: $where) {
    results {
      id
      key
      masterData {
        current {
          nameAllLocales {
            locale
            value
          }
        }
      }
    }
  }
}
