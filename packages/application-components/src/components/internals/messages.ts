import { defineMessages } from 'react-intl';

const messages = defineMessages({
  back: {
    id: 'Components.Page.TopBar.Back',
    defaultMessage: 'Back to list view',
  },
});

export default messages;
