query FetchApplicationsMenu {
  applicationsMenu {
    navBar {
      shouldRenderDivider
      key
      uriPath
      icon
      labelAllLocales {
        locale
        value
      }
      featureToggle
      menuVisibility
      permissions
      submenu {
        key
        uriPath
        labelAllLocales {
          locale
          value
        }
        featureToggle
        menuVisibility
        permissions
      }
    }
    appBar {
      key
      uriPath
      labelAllLocales {
        locale
        value
      }
      featureToggle
      permissions
    }
  }
}
