fragment projectFragment on Project {
  name
  key
  suspension {
    isActive
  }
  expiry {
    isActive
  }
}

query ProjectsQuery {
  user: me {
    id
    projects {
      results {
        ...projectFragment
      }
    }
  }
}
