query QuickAccessProductQuery($productId: String!) {
  product(id: $productId) {
    id
    masterData {
      staged {
        allVariants {
          id
          key
          sku
        }
      }
    }
  }
}
