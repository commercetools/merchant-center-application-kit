mutation DeleteCustomApplicationDeploymentPreview(
  $organizationId: String!
  $deploymentId: ID!
) {
  deleteCustomApplicationDeploymentPreview(
    organizationId: $organizationId
    deploymentId: $deploymentId
  ) {
    id
  }
}
