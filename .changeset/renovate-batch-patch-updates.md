---
'@commercetools-frontend/application-shell': patch
'@commercetools-frontend/application-components': patch
'@commercetools-frontend/mc-scripts': patch
---

Batch-consolidated patch/minor Renovate dependency updates.

Bumped `@flopflip/*` (combine-adapters, http-adapter, launchdarkly-adapter, memory-adapter, react-broadcast, types) from 15.1.7 to 15.1.11, a real `dependencies` entry of `application-shell` (all six) and `application-components` (`react-broadcast`). Bumped `@pmmmwh/react-refresh-webpack-plugin` from 0.6.1 to 0.6.2, a real `dependencies` entry of `mc-scripts`.

Other updates in this batch (`@testing-library/*`, `@manypkg/cli`, `@percy/cypress`, `@percy/puppeteer`, `@preconstruct/cli`, and three GitHub Actions digest pins) only touch devDependencies, root-level repo tooling, or private template packages, so they don't require a version bump here.
