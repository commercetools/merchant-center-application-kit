// Hand-mirror of `packages/l10n/supported-locales.js` and its `.d.ts`; update all three.
// The real file is CJS, read via `babel-plugin-preval`, which Vite doesn't run.
const supportedLocales = ['en', 'de', 'es', 'fr-FR', 'pt-BR'];

export default supportedLocales;
